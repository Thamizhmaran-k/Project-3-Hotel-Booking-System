spring.application.name=hotelbookingsystem

server.port=8080
qpp.base-url=http://localhost:8080

# --- Render PostgreSQL Database Connection ---
spring.datasource.url=jdbc:postgresql://dpg-d409duqli9vc73c2t0d0-a.oregon-postgres.render.com/hotel_booking_db_c6pn
spring.datasource.username=hotel_booking_db_c6pn
spring.datasource.password=R58URKDAhJQIgYAR1Ni1b43IALqM6vqB
spring.datasource.driver-class-name=org.postgresql.Driver

# --- SSL Configuration (REQUIRED for Render) ---
spring.datasource.hikari.data-source-properties.sslmode=require

# --- JPA Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Thymeleaf Configuration ---
# Disables caching for easier development (refresh browser to see changes)
spring.thymeleaf.cache=false

# --- Stripe Configuration ---
stripe.api.secret-key=${STRIPE_API_SECRET_KEY}
